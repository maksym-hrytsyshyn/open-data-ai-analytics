apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: open-data-ai
spec:
  selector:
    app: web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
  type: NodePort
